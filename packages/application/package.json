{
  "name": "@quietops/application",
  "version": "0.0.0",
  "private": true,
  "description": "Evaluation orchestration and browser-facing projections for QuietOps.",
  "license": "MIT",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "demo:ledger": "npm run build && node ../../scripts/run-with-sqlite.mjs dist/src/demo-cli.js",
    "demo:github": "npm run build && node ../../scripts/run-with-sqlite.mjs dist/src/live-github-cli.js",
    "pretypecheck": "npm run build --workspace @quietops/contracts && npm run build --workspace @quietops/adapters && npm run build --workspace @quietops/agent && npm run build --workspace @quietops/storage",
    "test": "npm run build && node ../../scripts/run-with-sqlite.mjs --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quietops/adapters": "0.0.0",
    "@quietops/agent": "0.0.0",
    "@quietops/contracts": "0.0.0",
    "@quietops/storage": "0.0.0"
  },
  "devDependencies": {
    "@strands-agents/sdk": "1.13.0"
  }
}
